# Copy this file to `.env.local` (never commit `.env.local`).
# On Vercel / Netlify: add the same keys in Project → Environment Variables.

# Example — only if you later add server-only or NEXT_PUBLIC_ vars:
# DATABASE_URL=
# NEXT_PUBLIC_SITE_URL=https://your-domain.vercel.app
